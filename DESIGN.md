# TalkFlow 설계 초안

## 1. 목적

TalkFlow는 macOS용 개인 메신저 보조 앱이다. 사용자가 선택한 카카오톡 계정과 채팅방을 대상으로 새 대화를 수집하고, 문맥을 바탕으로 AI 답변을 생성하며, 채팅방별 규칙과 macOS 사용 상태에 따라 답변을 전송한다.

초기 제품은 개인용 사이드 프로젝트로 시작하되, 이후 다른 사용자가 설치해 쓸 수 있는 배포형 macOS 앱을 목표로 한다.

## 2. 제품 원칙

- 카카오톡 연결은 비공식 로컬 연동으로 취급하고, 나머지 앱 로직과 분리한다.
- 대화 원문, 검색 인덱스, 정책은 기본적으로 사용자의 Mac에 보관한다.
- AI 제공자와 비용은 사용자가 직접 선택하고 부담한다.
- 자동응답은 명시적으로 허용한 채팅방에서만 동작하며, 기본값은 꺼짐이다.
- 채팅방 표시 이름은 UI용이고, 내부 정책 연결은 계정 식별자와 채팅방 ID를 사용한다.
- 카카오톡 연결 상태나 계정 식별이 불확실하면 읽기·전송을 중지하는 안전 우선 정책을 적용한다.

## 3. 사용자 경험

앱은 메뉴 막대 앱과 설정 창으로 구성한다.

첫 실행 흐름:

1. 필요한 macOS 권한을 안내한다.
2. 카카오톡 연결 상태를 확인한다.
3. 현재 로그인된 계정을 감지하고, 사용자가 이 계정을 TalkFlow 프로필로 등록한다.
4. 지원 AI 제공자와 인증 방식을 선택한다. 기본 Codex 연결은 앱이 로그인 상태를 확인하고, 로그인이 없으면 브라우저 로그인 흐름을 시작한다.
5. 채팅방 목록을 동기화하고, 방별 응답 규칙을 설정한다.

일상 사용 흐름:

1. 새 메시지를 감지한다.
2. 해당 계정과 채팅방의 정책을 확인한다.
3. 응답 후보인 경우에만 최근 문맥과 요약을 AI에 전달한다.
4. AI가 응답 여부와 답변을 한 번에 반환한다.
5. 허용된 자동 전송 조건이 충족될 때만 전송 대기열을 처리한다.

앱 동작 방식:

- 로그인 시 자동 실행은 기본적으로 꺼져 있으며, 환경 설정에서 사용자가 켤 수 있다.
- 창의 닫기 버튼은 설정 창만 닫고, TalkFlow는 메뉴 막대에서 계속 실행된다.
- 실제 종료는 메뉴 막대의 `TalkFlow 종료`로 명시적으로 실행하며, 종료 시 감지·AI 호출·전송 대기열 처리가 모두 중단된다.
- 메뉴 막대와 기본 화면에는 전역 `응답 켬/끔` 상태와 긴급 중지 버튼을 항상 노출한다.

### 3.1 오해할 수 있는 설정에는 `?`를 둔다

이름만으로 동작을 알 수 없는 설정에는 이름 옆에 `?`를 두고, 누르면 설명을 띄운다. 설명이 필요한 이유는 이 설정들이 실제로 사람을 속였기 때문이다. `멘션에만 응답`으로 둔 방이 하루 종일 조용했고 — 등록한 말을 아무도 쓴 적이 없었다 — `낮음`과 `보통`은 그림에만 있는 단계였으며, 어떤 설정이 메시지마다 값을 치르는지는 화면 어디에도 없었다.

설명은 네 가지를 이 순서로 답한다. **무엇을 하나 · 언제 적용되나 · 비용 · 하지 않는 것.** 마지막 항목이 가장 오래 걸린 오해를 끝낸다. 끼어들기 확률은 1:1 방에서 아무 일도 하지 않고, 적극성은 끼어드는 빈도를 바꾸지 않는다. 그 문장이 없으면 사용자는 설정을 바꿔 가며 그 사실을 알아내야 한다.

답할지를 정하는 설정이 셋이 되면서 `?` 하나가 더 필요해졌다. 세 설정은 서로 다른 축에 있고, 축을 헷갈리면 엉뚱한 손잡이를 돌린다. 그래서 각 카드가 나머지 둘을 이름으로 가리킨다.

| 설정 | 답하는 질문 | 축 |
| --- | --- | --- |
| 답변 조건 | 무엇에 답할지 | 품질. 모델이 이 문장으로 판단한다 |
| 끼어들기 확률 | 얼마나 자주 물어볼지 | 비용. 무작위이고 이 Mac 안에서 끝난다 |
| 판단 주기 | 언제 물어볼지 | 비용. 모아 두었다가 한 번 묻고, 시간으로도 대화 수로도 센다 |

줄글로 쓰지 않는다. 이 설명은 한 번 잘못 읽힌 설정에 붙는 것이고, 문단은 두 번째에도 읽히지 않는다. 제목과 짧은 줄로 적어 눈으로 찾을 수 있게 한다.

`?`는 모든 설정에 붙이지 않는다. 이름이 곧 동작인 컨트롤 옆의 버튼은 설명이 아니라 소음이고, 버튼이 흔해지면 정말 필요한 자리의 버튼도 같이 무시된다.

설명 문구는 화면 본문이 아니라 값으로 둔다. 빈 설명이 열리는 `?`는 없는 것보다 나쁘고, 값이어야 테스트가 전부 훑을 수 있다.

## 4. 채팅방 정책

각 채팅방에는 다음 정책을 설정한다.

| 항목 | 값 |
| --- | --- |
| 응답 모드 | 끔 / 감지 전용 / 멘션에만 응답 / 자동응답 |
| 끼어들기 확률 | 0–100%. 기본값 100 |
| 답변 조건 | 자유 문장. 비워 두면 없음. 전역 값을 이 방만 덮어쓸 수 있다 |
| 응답 스타일 | 전역을 따름 / 이 방만 다르게. 기본값 전역 |
| 전송 방식 | 초안만 / 유휴 상태 자동 전송 / 상시 전송 |
| 판단 주기 | 즉시 / 직접 적은 주기(5초–120분 또는 2개–20개, 범위 가능). 기본값 즉시 |
| 최소 응답 간격 | 채팅방별 설정. 판단 주기가 `즉시`일 때만 적용 |
| 최근 인간 응답 우선 | 켬 / 끔 |
| 이름 태그 | 켬 / 끔. 기본값 끔 |
| 답변 활성화 시간 | 항상 / 지정한 시간대. 기본값 항상 |
| 사진 함께 읽기 | 켬 / 끔. 기본값 끔 |
| 대화 기억 | 켬 / 끔. 기본값 켬 |
| 먼저 말 걸기 | 꺼짐 / 초안만 / 전송까지. 기본값 꺼짐 |
| 먼저 말 걸기 주기 | 직접 적은 주기(10분–24시간, 범위 가능). 기본값 30분~3시간 |
| 이 방에서만 쓸 키워드 | 목록. 기본값 비어 있음 |

단체방의 기본값은 `멘션에만 응답`이다. 개인방도 사용자가 명시적으로 켜기 전에는 자동응답하지 않는다. 끼어들기 확률의 기본값은 100%이지만 `자동응답`이 아닌 방에서는 아예 읽지 않는다. 켜는 스위치는 응답 모드 하나이고, 그 뒤에 조용히 0으로 앉아 있는 두 번째 손잡이를 두지 않는다.

`감지 전용`은 지금 `끔`과 하는 일이 같다. 파이프라인은 두 모드를 같은 자리에서 똑같이 건너뛰고, 대화 수집은 방 정책이 아니라 계정 단위로 돌아간다. 이름이 약속하는 "기록은 하되 답하지 않음"은 `끔`도 하는 일이다. `낮음`과 `보통`이 같은 값이던 것과 같은 종류의 문제이므로, 차이를 만들기 전까지 화면은 같다고 적는다. 있지도 않은 단계를 이름만으로 약속하지 않는다.

이름 태그는 방마다 정하고 기본값은 끔이다. 카카오톡 밖에서 `@이름`을 적는 것은 멘션이 아니라 글자라서, 상대가 앱에서 보는 강조나 알림이 생기지 않는다. 그러면 답의 첫머리를 읽는 사람이 이미 아는 이름에 쓰는 셈이므로, 그 값이 있는 방에서만 켠다. 1:1에서는 켜도 쓰지 않는다.

답변 활성화 시간도 방마다 정하고 기본값은 제한 없음이다. 시작이 종료보다 늦으면 자정을 넘는 하나의 시간대로 읽는다. 22:00–02:00은 빈 시간대가 아니라 다음 날 아침에 닫히는 시간대다. 시작과 종료가 같으면 하루 종일로 본다. 아무에게도 답하지 않는 방을 사용자가 스스로 알아내게 하는 쪽이 더 나쁘다.

시간대 밖의 보류는 활동 기록에 남기지 않는다. 쿨다운이나 "다른 사람이 먼저 답함"과 달리 이 침묵은 예상된 것이고, 창 밖 시간 내내 메시지마다 발생한다. 남기면 사람이 봐야 할 초안과 실패가 같은 이유의 행에 묻힌다.

사진 함께 읽기도 방마다 정하고 기본값은 끔이다. 지금 사진은 모델에게 `사진`이라는 낱말 하나로 도착한다. 그래서 사진을 두고 오간 대화는 주제를 통째로 잃는다. 켜면 그 방의 최근 사진을 최대 세 장까지 답장 요청에 첨부하고, 각 사진이 누가 언제 보낸 것인지 프롬프트에 적는다. 어느 메시지의 사진인지 말해 주지 않으면 그것은 대화에 자리가 없는 사진일 뿐이다.

이 설정은 [2장](#2-제품-원칙)에서 정한 것 하나를 의도적으로 어긴다. 대화는 사용자의 Mac에 둔다고 했는데 사진 파일은 AI 제공자로 나간다. 그래서 전역이 아니라 방별이고, 기본값이 끔이며, 켜는 자리에 무엇이 나가는지 그대로 적는다. 방마다 사진의 성격이 다르고 그 판단은 사용자만 할 수 있다. 앱이 대신 정할 일이 아니다.

세 장으로 묶는 이유는 문맥이 30개 메시지이기 때문이다. 그 30개가 전부 사진일 수 있어서 상한이 없으면 답장 한 번의 값이 정해지지 않는다. 업로드 크기도, 이미지 토큰도, 호출 전에 도는 추출도 그렇다. 최근 것을 고르는 이유는 판단 대상이 가장 최근 메시지라서다. 스무 개 전의 사진은 그때쯤 이미 말로 설명되어 있다.

사진은 이 Mac에 이미 받아 둔 것만 쓴다. 연결 도구는 로컬에 없는 사진을 카카오톡 서버에서 다시 받아올 수 있지만, 초안 하나를 만들자고 사용자 이름으로 카카오에 요청을 보내지는 않는다. 사진을 못 찾으면 그 사진만 빠지고 답장은 글로 만든다. 그림 하나를 잃는 것이 답을 잃을 이유는 아니다.

꺼낸 파일은 호출이 끝나면 지운다. 모델이 실패해도 지운다. 남의 대화에서 나온 사진이 디스크에 쌓이는 것은 이 앱이 하지 말아야 할 일에 정확히 해당한다.

## 4.1 응답 스타일: 전역과 방별 덮어쓰기

설정 화면에서 기본 말투, 답변 길이, 이모지 사용 정도, 적극성을 정한다. 이 값이 모든 방의 공통 지침이다.

**방마다 덮어쓸 수 있다.** 스위치 하나를 켜면 그 방은 자기 값만 쓰고, 끄면 다시 전역을 따른다. 기본값은 꺼짐이고, 이 기능이 생기기 전에 설정한 방은 전부 꺼짐으로 온다. 그 방들은 지금까지 전역 스타일로 답해 왔고, 오늘의 전역 값을 방마다 복사해 두면 이후에 설정을 고쳐도 따라오지 않는 방이 다섯 개 생긴다.

켤 때는 **전역 값을 그대로 복사해 넣는다.** 방별 스타일을 만지는 사람은 대개 전역에서 한 가지만 다르게 하고 싶은 것이고, 앱 기본값("친근하고 간결하게")에서 시작하는 칸은 사용자가 설정 화면에서 이미 정한 것을 조용히 되돌린다.

**키워드는 스타일이 아니다.** 방이 자기 스타일을 가져도 "이름 외 추가 키워드"는 전역 그대로 남는다. 그 목록은 답을 어떻게 쓰는지가 아니라 무엇을 부름으로 볼지를 정하고, 그 방 몫은 이미 "이 방에서만 쓸 키워드"에 따로 있다. 말투를 바꾸려고 켠 스위치가 답하는 메시지까지 바꾸면 그것이 이 앱이 반복해 온 실수다.

멤버별 응대 스타일은 여전히 이후 기능이다.

## 4.2 나를 부르는 말

멘션과 키워드는 다른 개념이다. 한동안 같은 목록이었고 그래서 `멘션에만 응답`이 동작하지 않았다. 내 이름으로 불렸다는 사실을 알려면 사용자가 자기 카카오톡 이름을 설정 화면에 직접, 남들이 쓰는 철자 그대로 적어 두어야 했다. 아무도 그렇게 하지 않는다. 이름은 앱이 알 수 있는 값이다.

- **이름**: 아카이브에서 이 계정이 보낸 메시지의 발신자 이름을 읽는다. 설정할 것이 없다. 이름을 바꾸면 가장 최근 것을 쓴다. 아직 아무것도 보내지 않은 계정에는 읽을 이름이 없고, 그때는 이름이 없는 것으로 둔다. 누군가 다른 사람의 이름을 대신 쓰면 남의 이름에 답하게 된다.
- **전역 키워드**: 설정에 등록하고 모든 방에 적용한다. 이름으로 덮이지 않는 말 — 봇 별명, 예전에 쓰던 이름 — 을 위한 것이다.
- **방별 키워드**: 한 방에만 적용한다. 그 방 안에서만 이 계정을 뜻하는 말이 있다. 전역에 등록하면 다른 모든 방이 그 말에 반응한다.

세 가지는 더해진다. 방이 아무것도 등록하지 않아도 이름과 전역 키워드로 반응한다.

이름은 키워드보다 엄격하게 대조한다. 키워드는 메시지 어디에 있든 맞는 것으로 본다. 한국어 조사가 명사에 바로 붙기 때문이고, 너무 자주 걸리는 키워드는 사용자가 적은 말이라 지우면 그만이다. 이름은 사용자가 고른 값이 아니다. 짧은 이름 하나가 평범한 낱말 안에서 걸리면 아무도 부르지 않은 방에 답이 나간다. 그래서 이름은 낱말이 시작하는 자리에서 시작해야 하고, 뒤에는 한글만 이어질 수 있다. 조사와 `@`는 그대로 통과하고, 더 긴 낱말의 일부로는 걸리지 않는다.

부르지 않은 메시지의 보류는 활동 기록에 남기지 않는다. 조용한 방마다 메시지마다 발생해서, 남기면 사람이 봐야 할 초안과 실패가 묻힌다. 대신 그 침묵을 방 설정 화면에서 설명한다. 이 방이 답하는 말이 무엇인지, 최근 대화에서 그 말로 부른 메시지가 몇 개였는지 보여 준다. 설정이 잘못된 방과 아무도 부르지 않은 방은 밖에서 보면 똑같이 조용하다. 그 둘을 사용자가 데이터베이스를 열어 보고 구분하게 둘 수는 없다.

## 4.3 끼어들기 확률과 답변 조건

아무도 나를 부르지 않은 메시지를 어떻게 할지가 이 절의 주제다. 여기까지 오는 데 세 번 틀렸다.

### 4.3.1 단계 셋은 왜 없어졌나

처음에는 `꺼짐 / 낮음 / 보통` 세 단계였고, `낮음`과 `보통`이 **같은 값**이었다. 엔진은 자발 개입이 `꺼짐`인지만 물었고, 낮음으로 둔 방은 보통으로 둔 방과 똑같이 메시지마다 모델을 불렀다. 화면은 그동안 두 단계가 다르다고 적고 있었다.

그래서 다음에는 반대로 갔다. 모델을 부르기 전에 이 Mac 안에서 정하기로 하고, `낮음`은 6자 이상이면서 질문이거나 내 말 바로 뒤에 온 메시지만 넘겼다. 질문 판정은 물음표와 어미 몇 개의 목록이었다. 그 규칙은 배포 몇 시간 만에 `낮음`인 방에서 이런 모양의 메시지를 소리 없이 버렸다.

> 한도 다 차고 쓰지도 않았는데 초기화됐네요. 다음 리셋 날짜만 하루 밀리는건가..

(원문 대신 같은 모양으로 옮겨 적었다. 카카오톡 원문은 저장소에 두지 않는다.)

누가 봐도 질문이다. `건가..`로 끝나는데 목록에는 `건가요`가 있었고 물음표는 없었다. 한국어는 어미 목록이 담을 수 있는 것보다 훨씬 많은 모양으로 묻는다. 목록에 하나를 더할 때마다 `낮음`은 `보통`으로 되돌아가고, 더하지 않으면 이런 메시지를 계속 버린다. 어느 쪽도 답이 아니다.

세 번째로 그 규칙을 걷어내고 단계를 프롬프트 문구로 옮겼다. 그러자 `낮음`과 `보통`은 다시 같은 값에 가까워졌다 — 호출 수가 같고, 차이는 앱이 대신 써 준 조심스러운 문장 하나였다. **그 문장은 애초에 앱이 사용자의 뜻을 추측한 것이다.**

**그래서 단계를 없앤다.** 세 단어가 약속하던 것은 사실 두 가지였고, 둘은 서로 다른 축에 있다.

- 얼마나 자주 모델에게 물어볼지 → **끼어들기 확률**. 숫자라서 거짓말을 할 수 없다. 40은 40이다.
- 무엇에 답할지 → **답변 조건**. 앱이 추측하는 대신 사용자가 자기 말로 적는다.

### 4.3.2 끼어들기 확률

**물어볼 확률이지 답할 확률이 아니다.** 100%여도 모델은 답하지 않기로 할 수 있고, 실제로 자주 그런다. `should_reply`는 스키마에 있고 [거절에는 이유가 함께 온다](#61-답하지-않기로-한-이유는-모델에게-묻는다). 이 손잡이가 정하는 것은 그 판단을 물어볼지 말지뿐이다.

- **0%**: 예전의 `꺼짐`. 부르지 않은 메시지는 모델에 도달하지 않는다. 이름이나 키워드로 부른 것만 답한다.
- **100%**: 기본값. 예전 `낮음`과 `보통`이 둘 다 하던 일이다.
- **그 사이**: 그 비율만큼만 넘긴다. 넘기지 않은 메시지는 모아 두지 않고 그냥 지나간다.

**부름은 확률보다 위다.** 0%로 두어도 이름으로 부르면 답한다. `꺼짐`이 그랬던 것과 같다.

**뽑기는 주입한다.** 시계나 주기 뽑기와 마찬가지로 요청에 실려 오고, 그래서 테스트가 "이번에는 0.39" 라고 말할 수 있다. 그리고 값은 난수가 아니라 **판단 대상에서 유도한다.** 방 ID와 이번 판단이 답하기 시작하는 메시지 ID를 섞어 `0..<1`을 만든다. 같은 방의 같은 판단은 몇 번을 다시 봐도 같은 수가 나온다.

그래야 하는 이유는 파이프라인이 한 판단을 여러 번 들여다보기 때문이다. [뒷말 대기](#721-뒷말-대기) 뒤에 방을 다시 읽고, 그 방이 또 바뀌면 다음 동기화에서 다시 읽는다. 매번 새로 뽑으면 40%인 방은 이미 답하기로 한 답을 마지막 순간에 열 번 중 네 번 버린다. 밖에서 보면 고장이다. 뒷말 대기로 새 메시지가 들어온 경우에는 **첫 번째 판단이 답하기로 한 메시지를 두 번째 판단에 그대로 넘겨 준다.** 대상이 바뀌었다고 동전을 다시 던지지 않는다.

해시는 직접 섞는다. 표준 해시는 프로세스마다 씨앗이 달라서, 앱을 다시 켜면 이미 지나간 메시지가 다른 답을 받는다. 주기 뽑기가 같은 이유로 같은 선택을 했고, 둘은 같은 섞기를 공유한다.

`0%`가 아닌 방의 보류는 활동 기록에 남기지 않는다. 부르지 않은 메시지의 보류와 같은 종류이고 같은 양이다. 다만 보류 이유는 따로 둔다. 아무도 부르지 않은 방과 사용자가 확률을 낮춘 방은 다른 침묵이다.

### 4.3.3 답변 조건

사용자가 자기 말로 적는다.

> 일정 잡는 얘기랑 나한테 직접 묻는 것 위주로. 잡담엔 끼지 마.

이 문장이 프롬프트에 들어가고, 모델은 이 조건으로 판단한다. `낮음`이 대신 써 주던 문장을 사용자 자신의 문장으로 바꾼 것이다.

- **전역이고, 방마다 덮어쓸 수 있다.** 방의 조건은 전역과 합쳐지지 않고 대체한다. 비어 있는 덮어쓰기도 덮어쓰기다 — "이 방은 조건 없이"는 말할 수 있어야 하고, 빈칸을 "전역을 따름"으로 읽으면 그 말을 할 방법이 없어진다.
- **부름에도 적용된다.** `낮음`의 지시는 자발 개입으로 시작된 판단에만 붙였다. 그것은 앱이 지어낸 조심성이라 사용자를 이름으로 부른 사람에게까지 적용할 근거가 없었다. 이것은 사용자 자신의 문장이고, "일정 얘기만"이라고 적은 사람은 자기 이름이 불렸을 때도 그렇게 뜻한다.
- **먼저 말 걸기에는 적용하지 않는다.** 그쪽은 답하는 일이 아니다. "잡담엔 끼지 마"를 말을 꺼내는 프롬프트에 넣는 것은 한 설정을 다른 설정으로 읽는 일이다.
- **길이는 300자로 묶는다.** 매 호출에 실려 나가는 문장이라 값이 붙고, 상한이 없는 칸은 말투 설정이 할 일까지 통째로 붙여 넣는 자리가 된다. 300자면 위 예시 같은 규칙을 여덟 줄쯤 적을 수 있고, 프롬프트가 스스로 쓰는 규칙 블록을 넘지 않는다. 칸은 넘긴 글자를 잘라 내지 않고 **거절한다.** 입력 중에 칸의 글자를 고쳐 쓰지 않는다는 규칙은 여기에도 그대로 적용된다.
- **울타리는 씌운다.** 사용자의 글이므로 지시로 읽는 것이 맞고, 그 안에서 "지시처럼 보이는 말"을 찾아내려 하지 않는다. 다만 `<conversation>` 태그만은 예외다. 그것은 프롬프트에서 구조를 지니는 유일한 토큰이고 쓸 수 있는 것은 빌더 하나뿐이다. 조건에 우연히 그 태그가 들어가면 프롬프트의 내용이 아니라 모양이 바뀌고, 그 고장은 밖에서 보면 "설정이 안 먹는다"로 보인다. 값은 0이고, 울타리가 건드리는 것은 태그뿐이라 평범한 한국어는 그대로 지나간다.

### 4.3.4 이 셋을 헷갈리지 않게 한다

답할지를 정하는 설정이 셋이 되었다. [3.1의 표](#31-오해할-수-있는-설정에는-를-둔다)가 축을 나눈다. 각 `?` 카드는 나머지 둘을 이름으로 가리키고, 비용 손잡이 둘은 서로가 무엇을 다르게 하는지 적는다. **확률은 메시지를 버리고, 주기는 모아 두었다가 한 번에 본다.** 둘 다 호출을 줄이지만 잃는 것이 다르다.

"어느 것을 써야 하나"를 사용자가 설정을 하나씩 돌려 보며 알아내게 두지 않는다. 이 앱은 이미 아무 일도 하지 않는 설정을 네 개 만들었고, 그때마다 하루씩 들었다.

## 4.4 판단 주기와 최소 응답 간격

방마다 **즉시** 또는 **주기마다**를 고른다. 주기가 있는 방은 메시지를 모아 두었다가 주기가 끝날 때 **한 번** 모델에게 묻는다. 메시지마다 한 번이 아니라 주기마다 한 번이다. 이 설정만이 TalkFlow가 무엇을 말하는지가 아니라 무엇을 쓰는지를 바꾼다.

주기는 **시간으로도 대화 수로도** 센다. 어느 쪽인지는 숫자 옆의 단위가 정하고, 그 선택이 왜 필요한지는 [4.4.2](#442-주기는-시간으로도-대화-수로도-센다)에 적는다.

기본값은 즉시이고, 앱이 알아서 주기를 켜지 않는다. 사용자가 끼어들기 확률을 낮췄다고 해서 모아도 좋다는 뜻은 아니다. 주기는 이 방이 언제 답하는가를 바꾸는 설정이고, 그것을 켜는 것은 사용자다.

[끼어들기 확률](#432-끼어들기-확률)과는 함께 적용된다. 둘 다 호출을 줄이지만 겹치지 않는다. 확률은 넘길지를 정하고, 주기는 넘기기로 한 것을 언제 물어볼지를 정한다. 확률에 걸러진 메시지는 모으지도 않는다.

주기는 **고르는 것이 아니라 적는다.** 고정 메뉴는 방마다 다른 대화 속도를 담지 못하고, 무엇보다 범위를 표현할 수 없다. 받는 값은 단위마다 다르고, 그 밖의 값과 숫자가 아닌 입력은 거절하고 이유를 그 자리에 적는다. 0은 어느 단위에서도 받지 않는다. 즉시는 별도의 선택지이고, 빈칸이나 0으로 즉시를 뜻하게 하면 "주기 없음"과 "아직 안 적음"이 같은 값이 된다.

시간의 하한이 5초인 것은 동기화 간격이 5초이기 때문이다. 그보다 짧은 기다림은 방을 다시 들여다보기 전에 이미 끝나 있어서, 켜도 아무것도 바뀌지 않는 설정이 된다. 상한을 두는 이유는 그 너머에서 이 설정이 이름값을 못 하기 때문이다. 모아 둔 대화는 어차피 최근 것들로 잘리고, 두 시간 뒤의 답은 그 대화에 대한 답이 아니다. 예전 고정 메뉴는 30분에서 멈추고 한 시간을 아예 내놓지 않았는데, **권하지 않는 것과 사용자가 적어 넣은 값을 거절하는 것은 다른 일이다.** 그래서 상한은 "권하지 않는 지점"이 아니라 "설정이 말한 대로 동작하지 않는 지점"에 둔다.

입력 중에 칸의 글자를 고쳐 쓰지 않는다. 거절한 값도 칸에 그대로 두고 이유만 옆에 적는다. 스스로 글자를 다시 쓰는 칸은 애초에 타이핑이 되지 않는다는 것을 키워드 칸과 설정 화면에서 이미 겪었다.

두 설정이 같은 것을 뜻하면 안 되므로 **판단 주기가 켜져 있으면 최소 응답 간격은 적용하지 않는다.** 둘 다 "이 방이 얼마나 자주 답하는가"를 제한한다. 최소 응답 간격은 그 값을 싸게 흉내 낸 것이고 — 간격 안에 온 메시지를 그냥 건너뛴다 — 판단 주기는 건너뛰는 대신 모아 둔다. 그래서 주기가 이긴다. 설정 화면에서도 주기가 켜지면 간격은 잠기고, 지금 무엇이 적용되는지 적는다.

주기가 끝나면 **그 사이에 온 메시지 전체**를 놓고 판단한다. 마지막 메시지만 보면, 주기 초반에 나를 부른 사람은 주기가 끝날 때 사라진다. `멘션에만 응답`인 방은 그렇게 모으다가 멘션에 영영 답하지 않게 된다.

주기의 기준점은 **마지막으로 답한 시각이 아니라 마지막으로 모델을 부른 시각**이다. 이 설정이 필요한 방에서는 호출 대부분이 아무것도 만들지 않고 돌아온다. 답한 시각을 기준으로 삼으면 그런 방은 계속 메시지마다 값을 치르면서, 밖에서 보기에는 답을 안 하는 방으로 보인다.

### 4.4.1 주기는 범위로 둘 수 있다

주기는 하나의 수가 아니라 **최소와 최대**로 적을 수 있다. `10초~5분`처럼 두면 주기마다 그 사이 어딘가에서 기다리고, `5개~15개`로 두면 주기마다 그 사이 어딘가의 개수를 센다. 두 끝을 같게 두면 고정 주기이고, 최대 칸을 비우면 최소와 같은 값으로 읽는다. 대부분의 방은 고정으로 충분하므로 같은 수를 두 번 적게 하지 않는다.

정확히 300.000초마다 답하는 상대는 무슨 말을 하든 시계만 보고도 사람이 아니라는 것을 알 수 있다. 정확히 열 번째 메시지마다 답하는 상대도 같다. 시계 대신 세어 봐야 드러날 뿐이고, 한 방을 오래 쓰는 사람은 결국 센다. 범위는 그 규칙성을 흩는다.

**한 주기의 값은 주기가 시작할 때 정해지고 그 주기 동안 바뀌지 않는다.** 이것이 이 기능의 함정이다. 파이프라인은 동기화마다 방을 다시 들여다보는데, 들여다볼 때마다 새로 뽑으면 범위는 최솟값으로 무너진다. 짧게 나온 뽑기가 먼저 발동하고, 길게 나온 뽑기는 만료될 기회를 얻지 못한다. 개수도 똑같이 무너진다. 적게 나온 뽑기가 먼저 차기 때문이다.

그래서 뽑은 수를 어딘가에 저장하지 않는다. **그 주기의 시작 시각에서 유도한다.** 시작 시각은 이미 기록하고 있는 값이고(마지막으로 모델을 부른 시각), 주기 내내 바뀌지 않으므로 몇 번을 다시 물어도 같은 답이 나온다. 저장한 수가 시계와 어긋나는 경우도, 재시작 뒤에 사라지는 경우도 없다. 기다릴 초와 셀 개수는 **같은 뽑기**에서 나온다. 하나는 그대로 쓰고 하나는 반올림할 뿐이다. 해시는 직접 섞는다. 표준 해시는 프로세스마다 씨앗이 달라서, 앱을 다시 켜면 진행 중인 주기가 이유 없이 앞뒤로 뛴다.

뽑기는 주입한다. 시계와 마찬가지로 엔진이 주변에서 알아서 가져오는 것이 아니라 요청에 실려 오고, 그래서 테스트가 "이번 주기는 범위의 절반"이라고 말할 수 있다.

**마감은 주기가 시작할 때 정해지고 움직이지 않는다.** 주기 중에 메시지가 와도 미뤄지지 않는다. 새 메시지마다 마감을 뒤로 미는 방식은 말이 끊기지 않는 방에서 영영 자기 마감에 닿지 못한다. 대화가 기다림의 끝을 좌우하게 두면 기다림이 스스로를 무효로 만든다는 것은 [7.2](#72-전송-게이트)의 45초 안정화 대기에서 이미 겪었다. 그때는 기다리는 동안 도착한 메시지가 초안을 취소시켰고, 여기서 같은 실수를 하면 마감이 뒤로 밀린다. 어느 쪽이든 값은 치르고 얻는 것은 없다.

한 주기 동안 방은 **모으기만 한다.** 모델을 부르지 않고, 초안을 만들지 않고, 활동 기록에 줄도 남기지 않는다. 마감이 지난 뒤 새 메시지가 오면 그때 쌓인 것을 한 번에 판단한다. 마감 자체가 판단을 일으키지는 않는다. 방은 바뀌었을 때만 다시 들여다보기 때문이다.

주기가 매번 달라지는 것은 밖에서 보면 고장과 구별되지 않는다. 10초~5분으로 두고 4분을 기다린 사람은 자기가 무엇을 보고 있는지 알 방법이 없다. 아무도 부르지 않아 하루 종일 조용했던 `멘션에만 응답` 방과 같은 문제다. 마감은 실재하는 고정된 시각이므로 그냥 **적어 준다.** 방 설정 화면이 이번 주기가 몇 시에 끝나는지 한 줄로 말하고, 그 뒤 새 메시지가 오면 판단한다는 것도 함께 적는다.

개수로 세는 주기에는 적을 시각이 없으므로 **이번 주기가 뽑은 개수**를 적는다. 남은 개수가 아니라 뽑은 개수인 이유는 둘이다. 남은 개수는 화면을 열 때마다 그 방의 대화를 다시 읽어야 하고, 사용자가 달리 알아낼 수 없는 쪽은 뽑은 개수다. 5개~15개인 방은 이번에 몇 개인지 모르면 12개짜리 주기를 멈춘 방으로 읽는다.

남은 시간을 세는 카운트다운은 두지 않는다. 정직하게 유지하려면 설정 화면을 초마다 다시 그리는 타이머가 필요하고, 조용히 낡아 버리는 카운트다운은 없느니만 못하다. 시각은 화면을 다시 그리지 않아도 계속 맞다.

모아 둔 대화가 길어지면 **오래된 것부터 뺀다.** 판단 대상은 가장 최근 메시지이고, 뒤로 밀려난 말은 대체로 이미 답을 받았거나 사람들이 다시 말했다. 뺀 개수는 프롬프트에 적는다. 앞부분이 잘린 대화를 잘렸다고 말하지 않으면 모델은 그것을 온전한 대화로 읽고, 못 본 앞부분까지 아는 척 답한다. 뺀 부분을 요약해서 넣는 것 — 진짜 대화 요약 — 은 별도 기능이며 여기 없다.

모으는 동안의 보류는 활동 기록에 남기지 않는다. 시간대 밖 보류와 같은 종류의 침묵이고 같은 양이다. 주기 내내 메시지마다 한 줄씩 쌓이면, 사람이 봐야 할 초안과 실패가 "이 방은 설정대로 모으는 중"이라는 줄에 묻힌다.

### 4.4.2 주기는 시간으로도 대화 수로도 센다

**방마다 대화 속도가 다르다.** 어떤 방은 5분에 수십 개가 오가고, 어떤 방은 하루에 몇 개가 전부다. 그래서 같은 `5분`이 방마다 다른 것을 뜻한다. 한 방에서는 스무 개를 모으는 주기이고 다른 방에서는 아무것도 모으지 않는 주기다. 사용자가 방마다 그 수를 다시 계산해야 하는데, 계산에 필요한 메시지 속도는 화면에 없다.

느린 방에서 시간 주기는 **스스로 무너진다.** 메시지 사이의 간격이 주기보다 길면 주기는 매번 이미 끝나 있고, 방은 결국 모든 글에 하나씩 답한다. 즉시로 두었을 때와 같은 일을 하면서 이름만 주기다. 이 설정을 켠 이유가 그것을 막으려던 것이었으므로, 그 방에서는 시간이라는 단위 자체가 틀린 것이다.

`10개`는 어느 방에서나 대화 열 개다. 그래서 단위를 하나 더 두는 것이 아니라 **다른 단위로 재는 것**이 맞다.

**무엇을 세는가: 다른 사람의 메시지만 센다.** 내가 보낸 말은 세지 않는다. 이유는 둘이고 두 번째가 결정적이다. 내가 그 방에서 말하고 있다는 것은 내가 직접 대응하고 있다는 뜻이라 답을 앞당길 이유가 없고, TalkFlow가 보낸 답장도 아카이브에는 내 메시지로 돌아온다. 그것을 세면 답 하나가 다음 답의 차례를 앞당긴다. 아무도 정하지 않은 박자가 생긴다.

**받는 값은 2개부터 20개까지다.** 1개는 즉시를 다른 말로 적은 것이고, 같은 것을 두 가지로 말할 수 있게 두는 것이 이 앱이 사용자를 헷갈리게 해 온 방식이다. 상한은 취향이 아니라 한계다. 주기가 셀 수 있는 것은 한 호출이 보는 대화 창(30개)까지이고, 그중 내가 보낸 것은 세지 않는다. 창 크기에 가까운 목표를 두면 내가 함께 말하는 방에서는 영영 채워지지 않고, 방은 아무 설명 없이 계속 모으기만 한다. 20개는 나머지 열 자리를 내 몫으로 남겨 둔 값이다.

**두 단위를 함께 걸 수는 없다.** "10개 또는 5분 중 먼저 오는 쪽"은 실제로 있을 법한 요구지만, 그렇게 두면 느린 방에서는 언제나 5분이 먼저 온다. 개수가 아무것도 정하지 못하고, 방은 다시 모든 글에 답한다. **이 기능이 고치려던 바로 그 동작이다.** 반대로 "둘 다 채워야"는 어느 쪽 단독보다도 방을 조용하게만 만들 수 있고, 개수 주기가 이미 조심해야 하는 위험을 하나 더 얹는다. 무엇보다 숫자 네 개와 결합 방식 하나짜리 설정은 돌려 보지 않고는 무엇을 하는지 알 수 없다. 이 앱은 아무 일도 하지 않는 설정을 이미 네 개 만들었다.

그래서 **단위가 곧 방식이다.** 숫자 옆의 초·분·개 중 하나가 지금 무엇으로 세는지이고, 그 자리 말고 다른 곳에서 이 선택을 하지 않는다. 설명 카드도 두 단위를 나란히 놓는 대신 각각이 무엇을 잃는지 적는다. **시계는 느린 방을 거의 즉시로 만들고, 개수는 조용한 방을 기다리게 한다.** 어느 쪽도 항상 옳지 않아서 사용자가 고른다.

개수로 두면 답이 늦어질 수 있다는 것은 숨기지 않는다. 방이 조용하면 주기도 끝나지 않는다. 그래서 방 설정 화면이 이번 주기가 몇 개를 기다리는지와 함께 그 사실을 한 줄로 적는다.

기존 방은 전부 **시간**으로 읽는다. `5분`으로 둔 방은 5개마다 답해 달라고 한 적이 없고, 즉시인 방은 애초에 숫자를 갖지 않는다. 즉시는 단위도 갖지 않는다. 단위만 다른 두 개의 즉시가 서로 다른 값이 되면, 같은 아무것도 아님을 말하는 방법이 두 가지가 된다.

## 4.5 먼저 말 걸기

지금까지 TalkFlow가 하는 일은 전부 **대답**이었다. 무언가 도착했고, 앱은 거기 답할지를 정했다. 이것은 아무도 묻지 않았는데 사람이 있는 방에 사용자 이름으로 말을 넣는 첫 기능이고, **묻지 않은 말이야말로 가장 잘못 읽히기 쉽다.**

그래서 기본값은 꺼짐이고, 방마다 따로 켜야 하며, 답장에 이미 걸려 있는 조건은 하나도 면제되지 않는다. 새 조건을 만들기 전에 있는 조건부터 전부 통과시킨다.

### 4.5.1 두 번의 허락

전송은 이 설정 안의 **두 번째 선택**이다. 방을 `상시 전송`으로 둔 것은 "내 대신 답해도 된다"는 허락이지 "내 대신 말을 걸어도 된다"는 허락이 아니다. 그 둘을 같은 것으로 읽으면 아무도 켠 적 없는 기능이 말을 시작한다.

- **꺼짐**: 기본값. 아무도 부르지 않으면 이 방에서 아무 말도 하지 않는다.
- **초안만**: 켤 때의 기본값. 방의 전송 방식이 무엇이든 사람이 눌러야 나간다.
- **전송까지**: 방의 전송 방식대로 나간다.

`전송까지`는 방의 전송 방식보다 관대해질 수 없다. 방이 `초안만`이면 이 값을 골라도 초안이다. 이 설정이 전송 허락을 넓히는 통로가 되어서는 안 되기 때문이고, 그래서 두 값의 **곱**으로 정의한다. 지금 이 조합이 무엇을 뜻하는지는 방 설정 화면이 한 줄로 적는다. 화면이 말하지 않으면 사용자는 자기가 무엇을 허락했는지 청구서가 아니라 남의 대화에서 알게 된다.

### 4.5.2 무엇이 이 기능을 막는가

순서대로, 각각 혼자서 막는다.

1. 전역 자동 응답 꺼짐
2. 계정 확인 실패
3. 이 설정이 꺼짐
4. 응답 모드가 `끔` 또는 `감지 전용`
5. 답변 활성화 시간 밖
6. 방에 최근 대화가 없음
7. 이 방에서 마지막으로 말한 것이 나
8. 방이 아직 조용하지 않음
9. 이번 주기의 차례가 아직 오지 않음

4번에 `멘션에만 응답`은 없다. 그 값은 **어느 메시지에 답하는가**를 정하는 설정이고, 여기서 자동응답을 요구하면 먼저 말 걸기를 켜는 한 번의 조작이 그 방이 남의 메시지를 대하는 방식까지 조용히 바꾼다. 단체방의 기본값이 `멘션에만 응답`이므로 그 결합은 특히 나쁘다. 두 결정은 따로 둔다.

7번은 답이 없던 말 걸기를 다시 걸지 않기 위한 것이다. 마지막 말이 내 것이라면 그것은 대개 아무도 답하지 않은 지난번 말이고, 거기 이어 말하는 것은 대화를 여는 것이 아니라 혼잣말을 늘리는 것이다.

### 4.5.3 "조용하다"의 정의

**마지막 메시지가 30분보다 오래됐을 때.** 사람이 보낸 것이든 내가 보낸 것이든 상관없이 방의 가장 최근 메시지 하나로 잰다.

더 짧게 두면 끼어들기가 된다. 사람들은 대화 중에 몇 분씩 예사로 비우고, 그 틈에 들어간 말은 아무도 끝내지 않은 이야기에 대고 하는 말이다. 훨씬 길게 두면 쉬는 방이 아니라 죽은 방에만 들어가게 되는데, 거기도 대화를 열 자리는 아니다.

전송 직전에 **한 번 더 본다.** 만들어 둔 말은 그 침묵 하나를 근거로 허락받은 것이라서, 대기하는 사이 누가 말하면 취소한다. 답장의 "대화가 이어짐"과 달리 **누가 말했는지는 따지지 않는다.** 답장에서 여러 사람이 말하는 것은 대화가 굴러가는 것이지만([7.2](#72-전송-게이트)), 여기서는 누구든 말했으면 이미 조용한 방이 아니다.

### 4.5.4 주기

[판단 주기](#44-판단-주기와-최소-응답-간격)와 같은 범위 타입을 쓴다. 두 번째 범위 타입을 만들지 않고 **경계값만 다른 값으로 넘긴다.** 파서가 둘이 되면 둘은 반드시 갈라진다. 다만 단위에 `개`는 내놓지 않는다. 이 주기가 재는 것은 침묵이고, 침묵 안에는 셀 메시지가 없다.

같은 함정도 그대로 적용된다. 이 방들은 10초마다 다시 들여다보이므로, 볼 때마다 새로 뽑으면 범위는 최솟값으로 무너진다. 그래서 **기다림을 그 주기의 시작 시각에서 유도한다.** 시작 시각은 저장하는 값이 아니라 이미 있는 두 값 중 나중 것이다: **이 방이 마지막으로 모델을 부른 시각**과 **이 방에서 마지막으로 오간 말의 시각.** 새 메시지가 시작점을 밀지만, 새 메시지가 왔다는 것은 이 주기가 세고 있던 침묵이 이미 끝났다는 뜻이므로 미는 것이 맞다.

하한은 **10분**이다. 판단 주기의 5초와 다른 이유로 정해진다. 저쪽은 방을 다시 볼 수 있는 최소 간격이 하한이지만, 이쪽은 사람이 새 화제를 꺼낼 법한 간격이 하한이다. 10초마다 주제를 여는 것은 무슨 말을 하든 사람이 아니다. 상한은 **24시간**이다. "하루에 한 번쯤"은 실제로 있을 법한 요구이고, 길게 둔다고 이 설정이 이름값을 못 하게 되지는 않는다.

기본값만 판단 주기와 다르게 **고정이 아니라 범위**로 둔다. 5분마다 답하는 것은 바쁜 사람으로 읽히지만, 30분마다 정확히 말을 거는 것은 시계만 보고도 사람이 아니라는 것을 알 수 있다.

### 4.5.5 조용한 방은 어떻게 검사되는가

파이프라인은 동기화가 **바뀌었다고 알려 준 방**만 들여다본다. 조용한 방은 정의상 바뀌었다고 알리지 않으므로, 이 기능이 필요한 바로 그 방에는 영원히 도달하지 않는다.

두 번째 타이머를 만들지 않고 **전송 대기열의 10초 폴링에 얹는다.** 그 반복은 이미 "알려 주는 것이 없는 조건"을 위해 돌고 있고 — 사용자가 자리를 비우는 것, 화면 잠금이 풀리는 것 — 조용한 방은 그 조건 하나가 더 늘어난 것이다. 타이머를 하나 더 두면 [7.3](#73-깨어-있기)에서 잡는 어서션이 같은 모양의 일을 두 번 깨우게 된다. 대기열을 돌리기 **전에** 훑어서, 이번에 만들어진 것이 10초를 더 기다리지 않게 한다.

다만 **훑기는 1분에 한 번만 한다.** 대기열이 10초인 것은 초안이 언제든 보낼 수 있게 되기 때문이지만, 조용한 방은 그렇게 빨리 바뀌지 않는다. 주기의 하한이 10분이고 그 앞에 30분의 침묵이 있으므로, 10초마다 묻는 것은 같은 답을 예순 번 받는 일이다. 한 번 볼 때마다 계정을 다시 확인하고 그것은 프로세스를 하나 띄운다. 이 앱은 몇 시간씩 가만히 있으라고 만든 것이다.

동기화가 초안을 넣은 뒤에 대기열을 한 번 더 돌리는 경로에는 얹지 않는다. 그 호출은 방금 어떤 방이 말했다는 뜻이고, 이 훑기가 찾는 것의 정반대다.

10초마다 도는 일이므로 **아무도 켜지 않았을 때 값이 0이어야 한다.** 그래서 맨 앞에 로컬 질의 하나를 둔다. 어느 방이든 이 설정이 켜져 있는가. 아니면 그 자리에서 끝난다. 계정 확인은 프로세스를 하나 띄우고, 방 목록과 대화 읽기는 아카이브를 읽는다. 전부 그 질문 뒤에 둔다.

### 4.5.6 주제는 그 방에서 나온다

주제는 **그 방의 최근 대화**에서 모델이 고른다. 오래 알고 지낸 사람들의 방에 아무 데서나 쓸 수 있는 인사를 던지면 첫 문장을 다 읽기 전에 봇인 것을 안다. 그래서 프롬프트는 "위 대화에서 실제로 오간 이야기에서 이어지는 말만"이라고 적고, 인사·안부·화제 전환용 질문을 이름을 들어 금지한다.

프롬프트는 답장 프롬프트의 한 갈래가 아니라 **별도의 빌더**다. 답장 쪽은 처음부터 끝까지 답할 메시지를 중심으로 쓰여 있다. `판단 대상` 표시가 있고, 판단의 계기를 적고, "마지막 메시지에 답할지 판단하고"로 끝난다. 여기서는 그 문장들이 전부 사실이 아니고, 하나씩 두 번째 뜻을 끼워 넣으면 하나의 프롬프트가 두 가지를 어중간하게 말하게 된다. **공유하는 것은 공유해야 할 것들이다.** 울타리, 신뢰할 수 없는 입력 규칙, 응답 스타일, 그리고 응답 스키마 — 판단 하나와 이유 하나라는 그 모양이 마침 이 기능에 정확히 맞는다.

**모델은 거절할 수 있어야 하고, 대부분은 거절이 맞다.** 그래서 프롬프트가 그렇게 적는다. 무언가를 만들라고 시킨 모델은 무언가를 만들고, 할 말이 없을 때 만들어지는 것이 바로 위에서 금지한 그 인사다. 거절 이유는 [6.1](#61-답하지-않기로-한-이유는-모델에게-묻는다)과 같은 칸에 같은 방식으로 적는다.

### 4.5.7 기록

활동 기록의 종류에 `먼저 말 걸기`를 더한다. 이 줄에는 **촉발 메시지도 답한 대화도 없다.** 답한 것이 없기 때문이고, 방의 마지막 메시지를 옆에 붙이면 그 메시지에 대한 답장으로 읽힌다. 이 기능에 대해 타임라인이 틀리면 안 되는 것이 정확히 그 한 가지다.

**두 결과가 같은 종류를 쓴다.** 모델이 만든 줄과 모델이 넘긴 줄 둘 다다. 넘긴 줄을 `보류`에 두면 거절당한 답장 수백 줄 사이에 섞여, "먼저 걸 말이 왜 안 나가지"를 알아보려면 모든 줄을 읽어야 한다. 대신 본문이 없는 줄은 결과 칸에 `말 걸지 않음`이라고 적는다. 말한 적 없는 줄에 `먼저 말 걸기`라고만 적으면 나간 메시지로 읽힌다.

필터에도 칸을 하나 더 준다. 수가 많아서가 아니라 — 오히려 가장 적다 — 이 화면에서 **아무도 요청하지 않았는데 앱이 쓴 유일한 줄**이라서다.

식별자는 메시지 id가 아니라 **이 줄만의 키**다. 초안이 처리되었는지는 `(방, 촉발 메시지 id)`로 판정하는데, 그 방의 마지막 메시지에는 이미 검토를 기다리는 답장 초안이 걸려 있을 수 있다. 같은 id를 쓰면 두 줄이 한 줄이 되고, 먼저 말 걸기를 보내는 순간 사람이 아직 보지 않은 답장 초안이 처리된 것으로 사라진다.

`먼저 말 걸기`는 모델 호출을 썼으므로 **주기의 기준점을 옮긴다.** 반면 답장은 아니므로 [최소 응답 간격](#44-판단-주기와-최소-응답-간격)은 시작시키지 않는다. 화제를 꺼내 놓고 거기 답한 사람을 5분 동안 무시하는 것은 처음부터 말을 걸지 않느니만 못하다.

**차례를 기다리는 동안의 보류는 기록하지 않는다.** 시간대 밖 보류나 주기 중 보류와 같은 종류이고 양은 더 많다. 10초마다 한 줄이면 그 줄이 설명하려던 화면을 그 줄이 덮는다.

## 5. 아키텍처

```text
Kakao Adapter
  ├─ 계정 확인
  ├─ 채팅방·메시지 동기화
  └─ 전송 자동화
        ↓
Local Store
  ├─ 계정·채팅방 정책
  ├─ 메시지 아카이브
  ├─ 대화 요약·기억
  └─ 전송 대기열
        ↓
Policy Engine
  ├─ 멘션·직접 질문 규칙
  ├─ 끼어들기 확률 뽑기 (판단 대상에서 유도, 모델 호출 없음)
  ├─ 방별 쿨다운과 판단 주기
  └─ 유휴·회의·잠금 상태 판단
        ↓
AI Provider Adapter
  ├─ Codex CLI
  ├─ OpenAI API
  └─ 향후 다른 제공자
        ↓
Send Coordinator
  └─ 조건 충족 시 Kakao Adapter에 전송 요청
```

### 5.1 Kakao Adapter

초기 기본 연결부는 `katok` 기반의 로컬 DB 읽기와 macOS 접근성 API 기반 전송으로 둔다. 여러 공개 도구에 기능을 분산하지 않고, TalkFlow가 이 하나의 연결부를 감싼다. 이 어댑터는 다음 기능만 제공한다.

- 현재 계정 식별자 조회
- 채팅방 목록 및 안정적인 채팅방 ID 조회
- 신규 메시지 증분 조회
- 채팅방에 텍스트 전송
- 연결·권한·동기화 상태 보고

카카오톡의 로컬 DB 형식이나 UI가 바뀌어도 TalkFlow의 정책, 저장소, AI 연결부는 영향을 최소화하도록 어댑터 뒤에 숨긴다.

`katok`의 동기화는 변경된 대화를 로컬 아카이브에 증분 반영한다. TalkFlow는 카카오톡 DB 파일 변경을 감지한 뒤 동기화를 호출하고, 반영된 새 메시지만 응답 파이프라인에 전달한다.

동기화는 신규 메시지가 0건이어도 원본을 전부 다시 읽으므로 비용이 일정하다. 따라서 대화가 활발할 때 연속 실행되지 않도록 동기화 사이에 최소 간격을 둔다. 이 간격은 자동 전송 전에 필요한 안정화 시간도 겸한다. 감지 대상에서 SQLite의 `-shm` 파일은 제외한다. 읽기만 해도 갱신되는 파일이라 동기화 자신의 읽기가 새 활동으로 보인다.

실측값과 확인 방법은 [플랫폼 실측 기록](docs/PLATFORM-FINDINGS.md) 4장에 있다.

### 5.2 계정 구분

카카오톡 로컬 데이터에서 얻는 계정 식별값을 `account_fingerprint`로 취급한다. 실제 구현에서 `user_id`와 `uuid` 같은 값이 가능하더라도, 앱의 다른 계층은 특정 값의 형식에 의존하지 않는다.

- 모든 데이터의 소유 범위는 `account_fingerprint`다.
- 채팅방 정책 키는 `(account_fingerprint, chat_id)`다.
- 시작 및 전송 직전에 현재 계정을 재검증한다.
- 등록한 계정과 다르면 동기화·전송을 중지하고 UI에 상태만 표시한다.

계정 재검증은 캐시 파일을 읽는 것으로 끝내면 안 된다. 카카오톡은 계정마다 데이터베이스를 따로 만들고, 연결 도구는 캐시된 식별값으로 파일 이름을 유도하므로, 캐시가 낡으면 **로그아웃한 계정의 대화를 계속 읽는다.** 그동안 전송은 UI 자동화라 현재 로그인된 계정으로 나간다. 옛 계정의 대화 맥락으로 만든 답장이 새 계정으로 전송될 수 있고, 지문을 계정 단위 키로 쓰기로 한 결정이 막으려던 사고가 정확히 이것이다.

그래서 계정 해석은 캐시가 아니라 디스크에서 시작한다.

- 카카오톡이 실제로 쓰고 있는 데이터베이스를 먼저 찾는다.
- 어떤 식별값이 그 파일 이름을 유도하는지 확인한다. 살아 있는 파일을 만들어내지 못하는 식별값은 지금 로그인된 계정이 아니다. 어느 캐시에서 나왔든 마찬가지다.
- 확인은 유도가 아니라 파일 이름 비교로 한다. 유도는 비싸고, 파일이 바뀌기 전에는 답이 달라지지 않는다.
- 해석된 식별값을 연결 도구에 환경 변수로 넘긴다. 넘기기 전에 다시 확인한다.
- 어긋나면 연결됨으로 보고하지 않는다. 아무 답도 안 하는 것이 틀린 계정의 대화로 답하는 것보다 낫다.

아카이브에는 여러 계정이 함께 쌓인다. 채팅방 목록과 메시지 조회는 반드시 계정 단위로 걸러야 한다. 거르지 않으면 지금 계정이 나눈 적 없는 대화를 근거로 답장이 만들어진다.

유도 방식과 식별값 복구 절차는 [플랫폼 실측 기록](docs/PLATFORM-FINDINGS.md) 1장에 있다.

초기 연결 도구가 현재 macOS 버전에서 계정 식별값을 자동 복구하지 못할 수 있다. 이 경우 연결 마법사는 사용자가 확인한 계정 ID를 로컬 Keychain에 저장해 연결을 재시도한다. 자동 감지 실패 상태에서는 대화 읽기·전송을 시작하지 않는다.

### 5.3 대화 기억

대화 기억은 세 층으로 구성한다.

- 최근 문맥: 답변 생성에 쓰는 최신 메시지 묶음
- 채팅방 요약: 관계, 진행 중인 주제, 약속, 반복되는 맥락
- 선택적 검색 인덱스: 오래된 대화를 의미 기반으로 찾기 위한 로컬 인덱스

앞의 두 층을 구현했다. 의미 검색은 데이터 크기와 품질 필요성이 확인된 뒤 추가한다.

#### 5.3.1 채팅방 요약

모델은 최근 30개만 본다. 몇 달을 이어 온 사이에서 그것은 거의 아무것도 아니다 — 이 사람들이 사용자에게 누구인지, 지난주에 무엇을 약속했는지, 둘이 지금 무엇을 하는 중인지가 전부 그 창 밖에 있다. 그래서 방마다 짧은 메모를 두고 답장 요청마다 함께 보낸다. 이 층은 방마다 켜고 끄며 **기본값은 켬**이다. 사진이나 먼저 말 걸기와 달리 밖으로 나가는 것이 넓어지지 않는다. 요약이 압축하는 대화는 이미 답장마다 제공자로 나가고 있다.

**고칠 수 있다.** 모델이 초안을 쓰고 사용자가 고친다. "前 직장 동료, 존댓말 유지" 같은 줄은 대화를 아무리 읽어도 유도되지 않고, 고쳐 둔 요약은 사람이 쓴 지침이며 봇은 그 지침으로 일한다.

**갱신은 증분이다.** 새 요약 = 모델(지난 요약 + 그 뒤에 온 대화). 전체 이력을 다시 읽지 않는다. 그래야 방이 오래돼도 기억의 값이 늘지 않는다. 요약이 없는 방의 첫 갱신도 전체가 아니라 최근 대화의 정해진 조각만 읽는다. 실측 기준은 [플랫폼 실측 기록](docs/PLATFORM-FINDINGS.md)에 둔다.

**답장 경로에서는 갱신하지 않는다.** 갱신은 자기 일정으로 도는 별도의 AI 호출이고, 답장이 그것을 기다리는 일은 없다. 새 메시지가 일정 수 쌓이거나 마지막 갱신에서 하루가 지나면 갱신하고, 방 화면의 버튼으로 언제든 지금 갱신할 수 있다. 그래서 이 갱신은 동기화 흐름이 아니라 전송 대기열의 순회에 얹는다. 동기화 핸들러 안에 모델 호출을 두면 다음 방의 답장이 그 뒤에 줄을 선다.

**길이를 묶는다.** 매 호출에 실려 나가므로 길이는 한 번이 아니라 호출마다 치르는 값이다. 대화 창이 4000자인데 배경 메모가 그 몇 분의 일을 넘어가면 방에서 실제로 오간 말보다 무거워진다.

**울타리를 씌운다.** 요약은 신뢰할 수 없는 대화에서 나온 모델의 출력이라, 메시지가 요약을 통해 지시를 심을 수 있다. 저장할 때 `ConversationFence`를 통과시키고, 프롬프트에서는 울타리 밖에 놓되 "지시가 아니라 배경 설명"이라고 적는다. 태그를 막는 것은 모양이고, 그 문장이 내용을 막는다.

**단체방과 1:1은 다른 물건이다.** 실측한 계정에서 가장 큰 방의 참여자는 82명이고, 그런 방에서 사람별 명단은 길이만 다 쓰고 답장에는 쓸모가 없다. 무엇보다 [5.4](#54-채팅방-인사이트-화면)가 긋는 선을 넘는다. 그래서 단체방 요약은 방을 설명한다 — 어떤 방이고, 어떤 말투로 오가고, 무엇이 진행 중인지. 사람 이름은 진행 중인 일에 필요할 때만 적고("누가 언제까지 하기로 했는지"), 사람됨은 적지 않는다. 1:1은 반대로 관계가 하나뿐이고 그 하나가 답장이 계속 틀리는 부분이라, 대화에서 밝혀진 관계·호칭·말투를 적는다.

**고친 요약은 자동으로 덮어쓰지 않는다.** 사람의 수정이 아무도 누르지 않은 사이에 사라지는 것이, 요약이 낡는 것보다 나쁜 실패다. 그래서 자동 갱신은 고친 요약을 건드리지 않고 지나가고, 화면이 그렇게 적는다. `지금 갱신` 버튼은 고친 요약도 갱신하되 — 사용자가 누른 것이므로 조용한 손실이 아니다 — 그 문장을 "그대로 두라"고 프롬프트에 함께 넣는다.

**저장은 별도 표에 한다.** 정책과 수명이 다르다. 정책 행은 방 화면이 글자를 칠 때마다 모든 칸을 다시 쓰고, 요약은 그 사이에도 도는 배경 갱신이 쓴다. 한 행이면 두 writer가 서로의 낡은 사본을 되돌려 놓는다. 지우기도 진짜 삭제여야 한다 — 실제 사람에 대한 기록이므로, 남은 행을 "빈 것으로 읽기로 하자"는 약속으로 두지 않는다.

**보이게 한다.** 방 설정 화면이 지금 무엇이 적혀 있는지, 언제 만들었는지, 메시지 몇 개를 반영했는지 보여 주고, 고치고 갱신하고 지울 수 있게 한다. 사용자가 볼 수 없는 요약은 동의한 적 없는 친구 이야기다.

### 5.4 채팅방 인사이트 화면

채팅방 관리 화면은 단순한 토글 목록을 넘어서, 사용자가 봇의 동작을 이해하고 조절할 수 있는 대시보드가 된다.

- 주요 주제: 최근 대화와 누적 요약에서 추출한 주제
- 대화량: 일·주 단위 메시지 수, 활발한 시간대, 최근 활동 변화
- 참여 현황: 메시지 수와 최근 활동을 기준으로 한 참여자 현황
- 대화 스타일 메모: 답변 길이, 존댓말·반말, 질문 선호처럼 대화에 직접 도움이 되는 관찰
- 봇 활동: 최근 판단, 답변, 보류 이유, 다음 자동 전송 대기 상태

인사이트는 다음 두 경로로 갱신한다.

1. 로컬 집계: 메시지 수, 시간대, 활동량은 새 메시지 동기화 때 즉시 계산한다. AI 호출이 필요 없다. 아직 남았다.
2. AI 요약: 기존 요약과 마지막 갱신 후 새로 쌓인 메시지만 사용한다. 일정 수 이상의 새 메시지, 마지막 갱신에서 하루 경과, 또는 사용자의 수동 새로고침 중 하나가 발생할 때만 실행한다. 구현했다 — [5.3.1](#531-채팅방-요약).

AI가 생성한 인사이트에는 생성 시각과 근거가 된 메시지 범위를 표시한다. 참여자에 대한 추정은 불확실한 보조 정보로만 취급하며, 민감한 개인 특성이나 사적인 속성은 추론·저장하지 않는다.

이 마지막 문장은 문서에만 두면 지켜지지 않는다. 프롬프트를 읽는 것은 모델이고, "이 사람들을 요약해"라는 요청에 모델은 성실하게 응한다. 그래서 같은 선을 요약 프롬프트의 규칙으로 다시 적는다. 대화에 실제로 나온 것만 적고, 성격·건강·종교·정치 성향·재산 같은 특성은 추론하지도 적지도 않으며, 전화번호나 계좌번호는 옮겨 적지 않는다. 단체방에서 사람별 설명을 만들지 않는 것도 여기에 속한다.

### 5.5 상호작용 타임라인

TalkFlow는 실제로 어떤 메시지에 어떤 판단을 내렸고 무엇을 전송했는지 확인할 수 있는 상호작용 이력을 제공한다. 이것은 과거 대화를 재실행하는 리플레이 테스트와 다른 기능이다.

- 전체 활동: 최근 감지, 보류, 답변 생성, 전송, 실패 이벤트
- 채팅방 보기: 해당 방에서 봇이 답한 흐름과 보류한 이유
- 멤버 보기: 특정 참여자의 메시지가 봇 응답을 촉발한 횟수, 그때 사용한 응답 방식, 최근 상호작용
- 상세 보기: 답한 대화, 사용한 문맥 범위, AI 판단 결과, 생성 답변, 실제 전송 결과

단체방의 답변은 방 전체로 전송되므로, 타임라인은 `전송된 채팅방`과 `응답을 촉발한 참여자`를 구분해서 표시한다. 멤버 보기는 성격을 단정하는 화면이 아니라, 관찰 가능한 대화 상호작용을 검토하는 화면이다.

### 5.5.1 한 줄이 아니라 답한 대화를 기록한다

기록은 답장 하나에 촉발 메시지 하나를 붙였다. 그 전제가 두 번 깨졌다. [뒷말 대기](#721-뒷말-대기)는 기다리는 동안 온 메시지를 같은 판단에 합치고, [판단 주기](#44-판단-주기와-최소-응답-간격)는 주기 사이에 쌓인 것을 한 번에 묻는다. 두 경우 모두 답장은 여러 메시지에 대한 것인데 기록에는 마지막 한 줄만 남아서, 나중에 읽으면 그 줄에서 그 답이 나오지 않는다.

그래서 **답한 대화**를 기록한다. 프롬프트에 실은 문맥 전체가 아니다. 그것은 주변 30개이고 개수로 따로 센다. 답한 대화는 더 좁다. 이 답이 무엇에 대한 답인지다.

정의는 도메인에 한 번만 둔다. **판단이 시작한 메시지부터 그 뒤에 오간 전부**다. 즉시인 방은 판단을 일으킨 메시지에서 시작하고, 뒷말 대기는 그 시작을 옮기지 않고 뒤에 붙인다. 주기마다인 방은 지난 호출 이후 쌓인 것 중 가장 오래된 메시지에서 시작한다. 두 경로가 같은 뜻을 만들려면 시작점이 판단 범위와 같은 값이어야 하므로, 기록은 정책 엔진이 판단에 쓴 그 범위의 첫 메시지를 그대로 받는다.

**본문을 함께 적는다.** 식별자만 남기면 아카이브가 정리된 뒤 아무것도 못 보여 준다. 촉발 메시지 본문을 판단 시점에 복사해 둔 것과 같은 이유이고([7.4](#74-초안-검토)), 같은 이유로 사람 이름도 그때 보이던 이름으로 적는다.

**상한을 둔다. 20개, 2000자.** 활발한 방의 5분 주기는 그보다 많이 쌓이고, 이 기록은 방을 재생하는 화면이 아니라 사람이 읽는 화면이다. 스무 줄이 넘으면 읽히지 않는다. 글자 수도 함께 거는 이유는 붙여 넣은 긴 글 하나가 짧은 스무 개보다 무겁기 때문이다. 넘치면 [프롬프트 창](#44-판단-주기와-최소-응답-간격)과 같은 방식으로 오래된 것부터 뺀다. **뺀 개수는 화면에 적는다.** 잘린 대화를 잘렸다고 말하지 않으면 온전한 것으로 읽히고, 그것은 이 기능이 없애려던 오독과 같은 종류다.

이 기록이 없던 시절의 줄은 촉발 메시지 한 줄로 그대로 보여 준다. 타임라인은 덧붙이기만 하므로 지난 줄에 답한 대화를 채워 넣을 방법이 없고, 빈칸으로 바뀌면 남아 있던 기록까지 잃는다.

## 6. AI 제공자와 인증

TalkFlow는 사용자 지참 키 또는 사용자의 로컬 인증을 사용한다. TalkFlow 운영 서버가 사용자 대화나 API 키를 보관하지 않는다.

- 기본 제공자: Codex CLI
- 추가 제공자: OpenAI API 및 향후 호환 가능한 AI 제공자
- 키 저장: macOS Keychain
- 모델 설정: 제공자는 프로필별로, 모델은 앱 전체에 하나로 선택([6.2](#62-어느-모델이-답하는지는-설정에-적는다))
- 인증 실패: 자동응답을 중단하고 UI에서 재인증을 요청

Codex CLI 연결은 API 키 입력을 기본값으로 두지 않는다. 사용자가 기존 ChatGPT 계정으로 Codex CLI에 로그인하면, TalkFlow는 해당 CLI의 로그인 상태만 확인하고 답변을 요청한다. 인증 토큰이나 로그인 캐시를 TalkFlow가 직접 읽거나 복사하지 않는다. API 키 방식은 사용자가 선택한 다른 제공자 또는 Codex CLI의 대안 인증으로만 제공한다.

Codex 호출은 잠근 상태로 실행한다. 모델이 명령을 실행할 수 없게 하고, 대화 내용이 세션 기록에 남지 않게 하며, 응답을 해석이 아니라 파싱으로 받는다. 대화 본문은 프로세스 인자가 아니라 표준 입력으로 전달한다. 인자는 같은 기기의 다른 프로세스에 그대로 보인다. 구체적인 플래그는 [플랫폼 실측 기록](docs/PLATFORM-FINDINGS.md) 5장에 있다.

사진만 예외로 파일 경로를 인자로 넘긴다. 이미지는 표준 입력으로 보낼 수 없다. 경로는 내용이 아니고, 그 파일은 호출이 끝나는 즉시 지워진다.

모델에게 침묵을 요구하는 범위는 좁게 둔다. 송금·결제 요청과 계좌번호·비밀번호·인증번호 같은 민감 정보 요청은 사용자가 직접 답하게 하고, 사용자만 아는 사실은 지어내지 않게 한다. 되돌리기 어려운 내용을 전부 막으면 약속을 잡는 일상 대화까지 침묵해서 제품이 성립하지 않는다.

AI 호출은 한 번으로 끝낸다. 코드가 멘션, 방 정책, 쿨다운 같은 확정 규칙을 먼저 검사하고, 후보 메시지에 대해서만 AI가 아래 구조를 반환하도록 한다. 같은 메시지를 두 번 판단하지 않도록 촉발 메시지 기준으로 중복을 막는다. 동기화는 어느 방이 바뀌든 실행되므로, 이 방어가 없으면 같은 메시지에 대해 호출이 반복된다.

```text
should_reply: true | false
reply_mode: mention | direct_question | spontaneous
confidence: low | medium | high
reply_text: string | null
decline_reason: string | null
```

### 6.1 답하지 않기로 한 이유는 모델에게 묻는다

거절에는 적을 곳이 없었다. 그래서 모델이 답하지 않기로 할 때마다 활동 기록에 같은 문장 하나가 남았다. 실제 저장소에서 세어 보니 그 문장이 146줄 똑같이 쌓여 있었다(2026-08-09, `SELECT COUNT(*) FROM agent_actions WHERE detail LIKE 'AI가 답하지%'`). 밖에서 보면 조용한 방과 판단이 나쁜 방이 구별되지 않는다.

[답할지의 판단을 모델에게 맡긴](#43-끼어들기-확률과-답변-조건) 뒤로는 그 구별이 더 중요해졌다. 이제 거절은 코드가 정한 것이 아니라 모델이 내린 판단이고, 판단의 질이 이 앱이 파는 것이라면 사용자가 그 판단을 읽을 수 있어야 한다. [답변 조건](#433-답변-조건)을 쓴 사용자에게는 더 그렇다. 자기가 적은 조건 때문에 걸러졌는지를 읽을 수 있어야 한다.

- **스키마 위치**: `reply_text`와 같은 모양으로 둔다. 응답은 해석이 아니라 파싱이고 스키마가 열거하지 않은 필드는 아예 돌아올 수 없으므로([플랫폼 실측 기록](docs/PLATFORM-FINDINGS.md) 5장), 한쪽 갈래에서만 뜻이 있는 값도 `["string","null"]`로 두고 required에 넣는다. 답장을 만든 호출은 `null` 하나를 더 낼 뿐이다.
- **길이**: 프롬프트에 **40자**로 적고 기록에 넣을 때 **60자**로 자른다. 물어보지 않은 상한은 매번 잘라 내는 상한이 되고, 자르지 않는 상한은 상한이 아니다. 두 수를 벌려 두는 것은 몇 글자 넘긴 정상 답변까지 자르지 않기 위해서다. 잘린 줄은 잘렸다고 표시한다.
- **비어 있을 때**: 예전 줄에는 이유가 없고 [타임라인은 덧붙이기만 하므로](#551-한-줄이-아니라-답한-대화를-기록한다) 앞으로도 없다. 모델이 빈 값을 돌려줄 수도 있다. 둘 다 원래의 고정 문장으로 남긴다. 빈칸으로 바뀌면 남아 있던 설명까지 잃는다.
- **답장에는 붙이지 않는다**: 성공한 줄은 답장 본문을 이미 들고 있다. 왜 답했는지에 대해 답장보다 더 잘 말하는 문장은 없고, 자유 서술 필드 하나를 모든 호출에서 사서 그것을 다시 설명하게 할 이유가 없다.
- **모델이 쓴 글이다**: 이 값은 대화 본문과 같은 취급을 받는다. 프롬프트 울타리를 무력화한 뒤에 기록에 넣고, 화면에는 `AI 판단:`을 앞에 붙여 누가 쓴 문장인지 밝힌다. 같은 칸의 다른 문장은 전부 앱이 쓴 것이고, 쿨다운 보류와 모델 거절은 결과 열에서 똑같이 `보류`다.
- **저장**: 새 칸을 만들지 않고 기존 `detail`에 넣는다. 이 값은 그 칸에 있던 고정 문장을 대신하는 한 줄이고, 검색하거나 정렬하지 않는다. 칸을 나누면 "설명, 단 이 경우에만"이라는 두 번째 필드가 생기고 화면은 둘 중 무엇을 보여 줄지 매번 정해야 한다.

### 6.2 어느 모델이 답하는지는 설정에 적는다

이 문서는 처음부터 "제공자와 모델을 선택"한다고 적어 두었지만 모델 쪽은 구현되지 않았다. TalkFlow는 `--model`을 넘기지 않았고, 그래서 실제로 답하는 모델은 `~/.codex/config.toml`에 적힌 것이었다. 그 파일은 사용자가 터미널에서 하는 다른 작업의 것이다. **다른 프로젝트 때문에 Codex 설정을 바꾸면 카카오톡 답장 품질이 조용히 따라 바뀌고, 그 사실을 알 수 있는 화면이 없었다.**

- **어디서 고르나**: 설정 화면. 저장 버튼 아래, 자동 전송 위에 둔다. 이 앱의 설정 화면은 "무엇을 답하나 → 어떻게 보내나" 순서로 읽히고, 모델은 앞쪽에 속한다.
- **기본값은 "선택 안 함"**: 플래그를 아예 넘기지 않고 Codex CLI 설정을 그대로 쓴다. 업그레이드로 누구의 답장도 달라지지 않는다. 빈 값을 넘기는 것과는 다르다 — [플랫폼 실측 기록](docs/PLATFORM-FINDINGS.md) 5.2절.
- **그 기본값의 대가를 화면에 적는다**: 고르지 않았을 때 무엇이 일어나는지를 선택 항목 아래에 쓴다. TalkFlow가 그 파일을 읽지는 않으므로 어떤 모델인지는 말하지 않는다. 읽지 않는 파일의 내용을 짐작해서 적으면 사용자가 그 파일을 고친 첫날에 거짓말이 된다.
- **고르는 즉시 저장한다**: 말투나 답변 조건처럼 저장 버튼을 기다리지 않는다. 이 값은 호출이 나가는 순간 읽히므로, 저장을 기다리는 값은 화면이 A를 가리키는 동안 B가 답하는 상태를 만든다. 전송 동의 스위치와 같은 이유다.
- **방마다 다르게는 두지 않는다**: 답장과 채팅방 요약이 같은 모델을 쓴다. 방별 덮어쓰기는 요청이 있을 때 판단한다.
- **목록은 실제로 돌려 본 것만 넣는다**: CLI에 모델을 열거하는 명령이 없어 목록은 CLI의 캐시에서 베낀다. 이름이 있다는 것은 동작한다는 증거가 아니므로([플랫폼 실측 기록](docs/PLATFORM-FINDINGS.md) 5.1절) 한 번씩 실제로 호출해 본 것만 남긴다. 폐기 예정으로 표시된 모델은 넣지 않는다. 곧 끊길 선택지를 주는 것은 날짜를 알려 주지 않은 고장이다.
- **목록에서 사라진 이름도 화면에서 사라지지 않는다**: 고른 이름이 목록보다 오래 살 수 있다. 그럴 때 그 이름을 이름 그대로 항목으로 두어 선택이 보이게 한다. 조용히 기본값으로 되돌리면 답하는 모델이 바뀐 것을 아무도 모른다.
- **추론 강도는 두지 않는다**: `codex exec`에 그 플래그가 없다. 설정 파일을 고쳐야 하고, 그 파일은 TalkFlow의 것이 아니다.

## 7. 자동 전송 정책

자동 전송은 되돌릴 수 없는 동작이므로 별도 대기열을 사용한다. 전송 직전에 계정·채팅방 ID·정책을 다시 확인하고, 실패·취소·전송 완료를 모두 활동 로그에 남긴다.

전송을 막는 유일한 조건은 **세션 잠금**이다. 디스플레이도 노트북 뚜껑도 아니다. 측정 결과는 [플랫폼 실측 기록](docs/PLATFORM-FINDINGS.md) 2장에 있다.

### 7.1 전송 방식

| 방식 | 동작 |
| --- | --- |
| 초안만 | 사람이 승인해야 나간다. 기본값 |
| 유휴 상태 자동 전송 | 자리를 비웠을 때만 나간다 |
| 상시 전송 | 기다리지 않는다 |

세 번째가 필요한 이유는 이렇다. 전송은 카카오톡 UI를 조작하는 일이고, 다른 데스크탑에 있는 창에 닿으려면 앱을 앞으로 가져와야 한다. 그래서 유휴를 기다리는 설계였는데, 그러면 **사람이 기기 앞에 있는 동안에는 영영 답장이 안 나간다.** 하루의 대부분이 그 시간이다. 방해를 감수할지는 사용자가 고른다.

대신 반드시 짝이 되어야 하는 것이 있다. **어디서든 즉시 멈출 수 있는 전역 단축키**다. 상시 전송이 화면을 가져가는 상황에서 TalkFlow 창을 찾아 눌러 끄는 것은, 바로 그 상황이 어렵게 만드는 일이다. 시스템 단축키로 등록해 입력 감시 권한 없이, 다른 앱이 키보드를 쥐고 있어도 동작하게 한다.

상시 전송이 없애는 것은 유휴 조건 하나뿐이다. 잠긴 화면에서는 여전히 보내지 않고, "다른 사람이 먼저 답함" 취소도 그대로 적용한다.

이 표는 **답장에 대한 것이다.** [먼저 말 걸기](#45-먼저-말-걸기)가 만든 말은 여기 더해 그 방의 먼저 말 걸기 설정도 `전송까지`여야 나간다. 답을 대신 하는 허락과 먼저 말을 거는 허락은 다르고, 이 표의 어느 값도 두 번째 허락을 대신하지 않는다.

화면을 가져갈지는 **볼 사람이 있는지**로 정한다. 뚜껑이 닫혀 있으면(그리고 외장 모니터가 데스크톱을 그리고 있지 않으면) 즉시 가져간다. 그때는 대가가 눈에 보이지 않고, 무엇보다 그 상태에서는 AX 창 목록이 비어 있어 기다리는 쪽을 고르면 아예 전송이 안 된다([3.4](docs/PLATFORM-FINDINGS.md)). 보는 사람이 있으면 짧게만 기다리고 실패한다. 실패는 재시도되므로 오래 기다릴 이유가 없다.

### 7.2 전송 게이트

대기열을 주기적으로 다시 검사한다. 기다리는 조건 대부분이 사용자가 자리를 비우거나 화면 잠금이 풀리는 것처럼 이벤트가 없는 변화이기 때문이다. 판정은 두 갈래다.

- **취소**: 전역 응답 꺼짐, 계정 불일치, 방 정책이 자동 전송이 아님, 대기 중 다른 사람이 답함, 초안이 너무 오래됨. 다시 맞는 상태가 될 수 없으므로 대기열에서 뺀다.
- **대기**: 이용 정책 미동의, 안정화 시간 미경과, 사용자가 입력 중, 화면 잠김. 나중에 충족될 수 있으므로 남겨 둔다.

취소를 대기보다 먼저 검사한다. 절대 나갈 수 없는 초안이 "안정화 중"이라고 표시되면 안 된다.

안정화 시간의 기본값은 0이다. 45초를 두었더니 **기다림 자체가 취소 사유를 만들어 냈다.** 취소가 대기보다 먼저 검사되므로, 안정화 창 안에 도착한 메시지는 곧바로 "대화가 이어짐"이 된다. 메시지가 25초 간격으로 오는 단체방에서 큐에 들어간 7건이 전부 4~12초 만에 취소됐고 45초 만기에 도달한 것은 하나도 없었다. 기다림이 지키려던 초안을 기다림이 죽였다. 게다가 초안 하나마다 모델 호출이 이미 나간 뒤였다. 대신 초안이 큐에 들어가는 즉시 대기열을 한 번 돌린다. 다음 폴링을 기다리면 그 사이에 대화가 또 움직인다.

전송 실패도 재시도 가능 여부로 나눈다. 연결 도구가 "아무것도 보내지 않았으니 재시도하라"고 답하면 큐에 그대로 두고, 실행 파일 부재처럼 다시 시도해도 같은 결과인 경우만 실패로 확정한다. 전자를 실패로 처리하면 사용자가 보내라고 한 메시지가 조용히 사라진다.

재시도 가능한 실패 중 **닫힌 대화창**만은 따로 다룬다. 다시 시도해도 사람이 그 방을 열기 전에는 성공하지 않는다. 실제로 답장 하나가 10분 동안 나가지 못했고, 그 이유는 대기열 테이블의 한 칸에만 있었다. 그래서 이 실패는 초안을 검토하는 화면(7.4)에 **무엇을 해야 하는지 적힌 한 줄**로 남긴다. 초안은 대기열에 그대로 두고 — 창을 열면 나가야 하므로 — 같은 이유가 계속되는 동안은 한 번만 남긴다. 대기열은 몇 초마다 다시 도는데 시도마다 한 줄이면 그 줄이 설명하려던 타임라인을 그 줄이 덮는다.

방을 **대신 열어 주지는 않는다.** 닫힌 방을 여는 방법은 어느 것이든 화면을 가져가고([플랫폼 실측 기록](docs/PLATFORM-FINDINGS.md) 3.6), 그것은 사용자가 아직 고르지 않은 별개의 결정이다.

### 7.2.1 뒷말 대기

답이 준비돼도 대화가 아직 끝나 보이지 않으면 몇 초 기다린다. 사람은 한 생각을 여러 메시지로 끊어 보내고, 남의 문장 앞부분에 칼답하는 것은 늦게 답하는 것보다 나쁘게 읽힌다.

**이 기다림은 전송 앞이 아니라 모델 호출 앞에 있다.** 위에서 45초 안정화가 실패한 이유가 그대로 여기 적용되기 때문이다. 전송 앞에 두면 기다리는 동안 도착한 메시지가 "대화가 이어짐"이 되어 초안을 취소시키고, 그 초안은 이미 값을 치른 뒤다. 호출 앞에 두면 취소할 초안이 아직 없고, 도착한 메시지는 **답할 대상을 바꾼다.** 즉 뒷말은 초안을 죽이는 것이 아니라 초안을 다시 만들게 한다. 이 순서가 아니면 이 기능은 만들지 않는 편이 낫다.

끝나 보이지 않는다는 판단도 로컬이다. 같은 사람이 몇 초 안에 연달아 말했거나, 문장이 연결어미나 쉼표로 끝났을 때다. 다른 사람이 빠르게 말하는 것은 해당하지 않는다. 그것은 한 문장이 이어지는 것이 아니라 대화가 굴러가는 것이고, 그것까지 세면 단체방의 모든 답이 기다림 뒤에 선다.

기다림은 **판단당 한 번**이다. 두 번째로 읽은 결과를 그대로 쓴다. 계속 치는 사람이 있는 방에서 뒷말마다 다시 기다리면 그 방은 자기 기다림의 끝에 영영 도달하지 못한다.

전송 이용 정책 동의는 사용자만 켤 수 있고, 켜기 전에는 어떤 메시지도 나가지 않는다. 동의 여부는 전송 시점에 다시 읽는다. 취소하면 다음 전송부터 즉시 막힌다.

macOS 상태는 권한 요청이 필요 없는 공개 API만 쓴다. 마지막 입력 이후 경과 시간, 화면 잠금, 뚜껑 상태 셋이다. 회의·화면 공유 감지는 공개 API로 판단할 방법이 없어 넣지 않는다.

### 7.3 깨어 있기

자동 응답이 켜져 있는 동안 두 가지 전원 어서션을 잡는다.

- `NoIdleSleep`: 맥이 잠들면 감지도 AI 호출도 멈춘다. 항상 유지한다.
- `NoDisplaySleep`: **뚜껑이 닫혀 있을 때만** 유지한다. 닫힌 뚜껑 뒤에서는 눈에 보이는 대가가 없고, 디스플레이가 자지 않으면 세션도 잠기지 않아 전송이 계속 가능하다. 뚜껑이 열리면 즉시 놓는다. 보이는 화면은 정상적으로 꺼져야 한다.

자동 응답을 끄면 둘 다 놓는다. 뚜껑을 닫은 채 계속 깨어 있으면 키보드로 방열이 되지 않아 열이 갇히므로, 가방에 넣기 전에 끄는 경로가 메뉴 막대에 있어야 한다.

### 7.4 초안 검토

전송 방식 기본값이 `초안만`이므로 대부분의 답변은 사람의 승인을 거친다. 따라서 밀린 초안을 검토하고 보내는 경로가 반드시 있어야 한다. 없으면 초안 생성 단계 전체가 쓸 수 없는 결과물만 남긴다.

검토는 **활동 화면에서 한다.** 별도 탭으로 두었더니 TalkFlow가 한 일을 보여 주는 목록은 그중 아무것도 손댈 수 없고, 손댈 것이 쌓이는 탭은 아무도 보지 않는 쪽이었다. 같은 목록이다. 종류와 채팅방으로 걸러 보게 하고, 기다리는 초안의 수는 목록을 열지 않아도 보이게 한다.

검토 화면에서의 전송은 사용자가 그 자리에 있다는 뜻이므로 유휴 판단을 적용하지 않는다. 계정 재검증은 그대로 한다. 한 계정에서 만든 초안이 다른 계정으로 나가면 안 된다.

전송 실패는 초안을 소진하지 않는다. 전송이 실패하는 이유는 대부분 지나가는 화면 상태라 다시 누르면 되고, 실패했다는 기록은 남되 초안은 목록에 그대로 있어야 한다.

지나가지 않는 실패는 여기서 말로 설명한다. 닫힌 대화창이 지금 유일한 경우다([7.2](#72-전송-게이트)). "전송에 실패했습니다"는 사용자가 할 수 있는 일을 알려 주지 않으므로, 그 자리에 이 방의 대화창을 열어 두어야 한다고 적는다. 조용히 15분 동안 재시도하다 초안이 만료되는 것이 지금까지의 동작이었다.

초안에는 그 답이 무엇에 대한 것인지가 반드시 붙어야 한다. 촉발 메시지의 본문과 보낸 사람 이름을 판단 시점에 함께 기록한다. 식별자만 남기면 아카이브가 정리되거나 계정이 바뀐 뒤에는 무엇에 답했는지 알 수 없고, 그 초안은 사람이 판단할 근거가 없다. 보류 기록에도 같은 이유로 붙인다. 답장이 여러 메시지에 대한 것일 때 무엇을 남기는지는 [5.5.1](#551-한-줄이-아니라-답한-대화를-기록한다)에 있다.

무시한 초안은 지우지 않고 무시했다는 기록을 남긴다. 무엇을 제안했고 사람이 무엇을 거절했는지가 타임라인에 남아야 한다.

### 7.5 그 외

부르지 않은 메시지에 끼어드는 것은 방별 빈도 제한과 최근 응답 후 침묵 시간을 반드시 함께 둔다. 무엇이 무엇을 정하는지는 [4.3](#43-끼어들기-확률과-답변-조건)에 있다.

전역 응답 상태가 꺼져 있으면 메시지 수집과 활동 기록은 선택적으로 유지할 수 있으나, AI 호출과 자동 전송은 수행하지 않는다. 메뉴 막대 아이콘은 정상 동작, 일시 중지, 연결 오류 상태를 구분해서 표시한다.

## 8. 데이터 모델 초안

- `profiles`: 사용자 정의 프로필 이름, 계정 지문, 선택한 AI 제공자
- `response_style_settings`: 전역 말투, 답변 길이, 이모지 사용, 적극성, 답변 조건
- `chat_rooms`: 계정 지문, 채팅방 ID, 현재 표시 이름, 개인방·단체방 유형
- `members`: 계정 지문, 안정적인 발신자 ID, 현재 표시 이름, 프로필 이미지 메타데이터
- `room_policies`: 채팅방별 응답·전송 정책, 끼어들기 확률, 판단 주기, 먼저 말 걸기와 그 주기, 방별 답변 조건과 응답 스타일 덮어쓰기
- `messages`: 계정 지문, 채팅방 ID, 메시지 ID, 안정적인 발신자 ID, 표시 이름, 시각, 내용
- `room_summaries`: 채팅방별 요약, 갱신 시각, 사람이 고쳤는지, 어디까지 읽었는지(마지막 메시지 ID와 누적 개수)
- `room_insights`: 주요 주제, 대화 스타일, 요약 근거 범위, 갱신 시각
- `room_activity_stats`: 일·주 단위 대화량, 시간대별 활동, 참여자별 메시지 집계
- `member_conversation_notes`: 대화에 직접 유용한 말투·응답 선호 메모와 신뢰도
- `pending_sends`: 생성 답변, 생성 근거, 전송 예정 조건, 상태. 먼저 말 걸기로 만든 줄은 그때 방의 마지막 메시지를 함께 들고 있다
- `agent_actions`: 감지·보류·생성·먼저 말 걸기·전송·실패 이벤트, 채팅방, 촉발 메시지, 답한 대화, AI 판단 결과, 생성 답변, 상태
- `member_interactions`: 참여자별 촉발 횟수, 최근 상호작용, 응답 방식 집계
- `activity_log`: 동기화·판단·전송·오류 이벤트

초안의 `action_contexts`는 두지 않는다. 그 표가 담기로 한 것 — 사용한 메시지 범위, AI 판단 결과, 생성 답변 — 은 이미 `agent_actions`의 칸들이고, 옆 표를 만들면 한 사실이 두 곳에 있게 된다. 답한 대화([5.5.1](#551-한-줄이-아니라-답한-대화를-기록한다))도 같은 행에 한 칸으로 둔다. 행과 1:1이고, 행을 쓸 때 한 번 같이 쓰고, 통째로 읽고, 조인하지 않고, 타임라인은 덧붙이기만 하므로 나중에 고칠 일도 없다. 메시지당 한 행으로 펼치는 것은 참여자별 집계를 그 표에서 뽑고 싶어질 때 다시 판단한다.

## 9. 개인정보·안전

### 9.1 권한 안내

권한은 필요한 기능을 사용하려는 시점에만 안내하며, 권한 센터 화면에서 현재 목적과 시스템 설정 이동 경로를 제공한다.

- 전체 디스크 접근: 카카오톡의 로컬 동기화 대화 읽기
- 접근성: 사용자가 자동 전송을 명시적으로 켠 경우에만 카카오톡 UI 조작
- 알림: 연결 오류·전송 실패 같은 선택적 상태 알림

전체 디스크 접근은 macOS 정책상 앱이 대신 허용할 수 없으므로, 설정 화면으로 이동시킨 뒤 다시 연결 진단을 실행한다. 접근성은 자동 전송을 활성화하려 할 때만 요청한다.

- 원문과 검색 인덱스는 로컬 저장이 기본이다.
- 사용자 API 키와 인증 토큰은 Keychain에만 저장한다.
- 외부 AI 제공자에 전달되는 대화 범위와 제공자를 설정 화면에서 명확히 보여 준다.
- 임의의 채팅 메시지는 신뢰할 수 없는 입력으로 취급한다. 메시지 안의 요청이 TalkFlow의 정책이나 시스템 지시를 바꾸게 해서는 안 된다.
- 설치 전과 권한 요청 전, 비공식 카카오톡 연동과 자동 전송의 제약을 명확히 고지한다.

## 10. 배포 전략

초기에는 개발자용 내부 빌드로 시작한다. 배포판은 macOS 앱 번들 안에서 필요한 보조 구성요소를 관리하고, 사용자가 터미널 명령을 직접 실행하지 않도록 한다.

배포판에 포함할 항목:

- 앱 서명과 공증
- 자동 업데이트
- 접근성·전체 디스크 접근 권한 설정 안내
- AI 제공자 연결 마법사
- 로컬 데이터 삭제와 내보내기 기능
- 카카오톡 연동 상태 진단 화면

## 11. 단계별 구현

단계 1부터 4까지는 구현하고 실제 대화로 검증했다. 단계 5는 남아 있다.

### 단계 1: 연결 검증 — 완료

계정 확인, 채팅방 목록, 메시지 증분 조회, 연결 상태 화면.

### 단계 2: 정책 제어판 — 완료

메뉴 막대, 채팅방별 응답 모드·전송 방식·쿨다운, 전역 응답 스타일, 전역 일시정지와 긴급 중지. 정책과 설정은 로컬 저장소에 남는다.

### 단계 3: AI 답변 초안 — 완료

Codex 제공자, 최근 문맥 전달, 구조화된 판단 반환, 판단·초안 활동 로그, 활동 화면에서의 초안 검토와 전송.

### 단계 4: 안전한 자동 전송 — 완료

전송 대기열, 유휴·잠금 판단, 전송 직전 재검증, 재시도 가능한 실패 구분, 전역 긴급 중지 단축키.

### 단계 5: 확장 — 남음

채팅방 요약과 증분 갱신은 이 단계에서 나왔다. 방마다 짧은 메모를 두고 답장 요청마다 함께 보내며, 갱신은 지난 요약과 그 뒤의 대화만 읽는다([5.3.1](#531-채팅방-요약)).

남은 것:

- 멤버별 대화 메모와 상호작용 집계
- 로컬 의미 검색
- 채팅방 인사이트 대시보드
- 멤버별 응대 스타일
- 다른 AI 제공자 어댑터
- 사용자용 설치·업데이트 흐름과 앱 번들 서명

## 12. 현재 결정 사항

- 대상 플랫폼은 macOS다.
- 카카오톡 개인 계정의 비공식 로컬 연동을 사용한다.
- 한 TalkFlow 프로필은 한 카카오 계정에 연결한다.
- 채팅방은 UI에서 명시적으로 관리한다.
- 단체방도 지원하되 기본값은 멘션 응답이다.
- 응답 여부와 답변은 단일 AI 호출에서 함께 결정한다.
- 무엇에 답할지는 사용자가 자기 말로 적고, 얼마나 자주 물어볼지는 숫자로 적는다. 단계를 뜻하는 낱말은 두지 않는다.
- 판단 주기는 시간으로도 대화 수로도 세되, 한 방에서는 한 단위만 쓴다. 방마다 대화 속도가 다르므로 어느 한쪽을 기본으로 정해 주지 않는다.
- 전역 설정을 방마다 덮어쓸 수 있게 하되, 덮어쓰지 않은 방은 전역을 계속 따른다.
- 채팅방 요약은 기본으로 켜고, 사용자가 읽고 고치고 지울 수 있게 한다. 갱신은 증분이고, 답장 경로 밖에서 돈다. 사람이 고친 요약은 자동으로 덮어쓰지 않는다.
- 사용자별 AI 키·인증과 비용 부담을 채택한다.
- 기본 AI 연결은 Codex CLI이며, 제공자는 교체 가능하게 설계한다.
- 어느 모델이 답하는지는 설정 화면에서 고르고, 고르지 않으면 Codex CLI 설정에 맡긴다. 목록에는 실제로 호출해 본 모델만 넣는다.
